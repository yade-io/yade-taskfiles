version: 3

silent: true

tasks:
  # ===============
  # === ANSIBLE ===
  # ===============
  test:connectivity:
    dir: "{{.PLAYBOOK_PATH}}"
    vars:
      PLAYBOOK_PATH: '{{default "." .PLAYBOOK_PATH}}'
    cmds:
      - echo ""
      - echo "🚀 tasks-ansible - test:connectivity [ STAGE - {{.STAGE}} ]"
      - 'echo "   cwd: $(pwd)"'
      - echo ""
      - ansible -i inventory/hosts_{{.STAGE}}.yml -m ping all
    requires:
      vars: [STAGE]

  inventory:print:
    dir: "{{.PLAYBOOK_PATH}}"
    vars:
      PLAYBOOK_PATH: '{{default "." .PLAYBOOK_PATH}}'
    cmds:
      - echo ""
      - echo "🚀 tasks-ansible - inventory:print [ STAGE - {{.STAGE}} ]"
      - 'echo "   cwd: $(pwd)"'
      - echo ""
      - ansible-inventory -i inventory/hosts_{{.STAGE}}.yml --graph --vars
    requires:
      vars: [STAGE]

  playbook:site:
    dir: "{{.PLAYBOOK_PATH}}"
    vars:
      PLAYBOOK_PATH: '{{default "." .PLAYBOOK_PATH}}'
      PLAYBOOK: '{{default "site.yml" .PLAYBOOK}}'
    cmds:
      - echo ""
      - echo "🚀 tasks-ansible - playbook:site [ STAGE - {{.STAGE}}, PLAYBOOK - {{.PLAYBOOK}} ]"
      - 'echo "   cwd: $(pwd)"'
      - echo ""
      - ansible-playbook -i inventory/hosts_{{.STAGE}}.yml {{.PLAYBOOK}}
    requires:
      vars: [STAGE]

  playbook:site:vars:
    dir: "{{.PLAYBOOK_PATH}}"
    vars:
      PLAYBOOK_PATH: '{{default "." .PLAYBOOK_PATH}}'
      PLAYBOOK: '{{default "site.yml" .PLAYBOOK}}'
    cmds:
      - echo ""
      - echo "🚀 tasks-ansible - playbook:site:vars [ STAGE - {{.STAGE}}, PLAYBOOK - {{.PLAYBOOK}}, VARIABLES - {{.VARIABLES}} ]"
      - 'echo "   cwd: $(pwd)"'
      - echo ""
      - ansible-playbook -i inventory/hosts_{{.STAGE}}.yml {{.PLAYBOOK}} -e "{{ .VARIABLES }}"
    requires:
      vars: [STAGE, VARIABLES]

  playbook:site:vars:tags:
    dir: "{{.PLAYBOOK_PATH}}"
    vars:
      PLAYBOOK_PATH: '{{default "." .PLAYBOOK_PATH}}'
      PLAYBOOK: '{{default "site.yml" .PLAYBOOK}}'
    cmds:
      - echo ""
      - echo "🚀 tasks-ansible - playbook:site:vars:tags [ STAGE - {{.STAGE}}, PLAYBOOK - {{.PLAYBOOK}}, VARIABLES - {{.VARIABLES}}, TAGS - {{.TAGS}} ]"
      - 'echo "   cwd: $(pwd)"'
      - echo ""
      - ansible-playbook -i inventory/hosts_{{.STAGE}}.yml {{.PLAYBOOK}} -e "{{ .VARIABLES }}" --tags "{{ .TAGS }}"
    requires:
      vars: [STAGE, VARIABLES, TAGS]
